# GTKY Librarian Batch Classification Implementation Plan

This implementation plan outlines the architecture and tasks required to scaffold the "Phase 2: Classify" logic for the GTKY Librarian Agent. This agent will take raw daily events from the MongoDB landing zone, read the "Hero Artifacts" (Origin and Ambitions), and use an LLM to accurately mint "Golden Objects" ready for Neo4j injection.

## User Review Required
> [!IMPORTANT]
> Please review the LLM prompting strategy and the payload routing described below. Do you want the GTKY Librarian to use CrewAI (for batching) or LangChain (Groq client directly) for this classification? (I have planned for a direct Groq/Langchain call for speed, but let me know if you prefer a different agent framework.)

## Proposed Changes

### [MODIFY] [gtky_librarian.py](file:///home/bizon/Programming/Agentic_workflows/Agentic_personal_porter/src/agents/gtky_librarian.py)
We will expand the `GTKYLibrarian` class to support a new method `classify_daily_batch`:
1. **Load Artifacts:** It will bundle `hero_origin.json` and `hero_ambition.json` into a system context block.
2. **Batch Iteration:** It takes a list of raw calendar events (the daily pulse).
3. **LLM Evaluation:** It prompts the LLM to classify each event into:
   - **Pillar/Subcategory** (e.g., "Career Goal", "Social")
   - **Intent vs. Actual** alignment
   - **Valuable Detours** (if the event was ad-hoc but positive)
4. **Mint Golden Object:** The outputs are returned as a list of validated JSON structures (Golden Objects) matching the `SovereignGraphInjector` requirements.

### [MODIFY] [sync_calendar_to_graph.py](file:///home/bizon/Programming/Agentic_workflows/Agentic_personal_porter/src/orchestrators/sync_calendar_to_graph.py)
We will update the Master Orchestrator to integrate Phase 2:
1. After pulling unstaged data from Mongo (Phase 1), the Orchestrator will hand the batch to the `GTKYLibrarian`.
2. The Librarian returns the "Golden Objects."
3. The Orchestrator saves these Golden Objects back to Mongo's `formatted_collection`.
4. Phase 3 proceeds as normal, injecting the formatted data into Neo4j using the existing `SovereignGraphInjector`.

## Open Questions

> [!WARNING]
> 1. **Batch Limits:** How many events do you expect per daily pulse? If it's a large number (e.g. 15+ events), we should batch the LLM calls into chunks of 5 to avoid context length or JSON formatting errors.
> 2. **LLM Model:** I plan to use Groq (`llama3-70b-8192` or `mixtral`) for fast JSON schemas. Which Groq model is preferred for the Librarian?
> 3. **Verification:** Are there any specific test events in the Mongo Database you want me to use for a dry run?

## Verification Plan

### Automated Tests
- Run `SovereignGraphInjector` on a mock Golden Object generated by the Librarian to ensure the `UNWIND` Cypher query in Phase 3 does not break.
- Validate that the output JSON strictly conforms to the expected Neo4j input parameters (e.g., `gcal_id`, `pillar`, `subcategory`, `duration_minutes`).

### Manual Verification
- Execute `python src/orchestrators/sync_calendar_to_graph.py` on a single day of data and visually inspect the nodes created in the Neo4j browser to ensure the `Event` -> `Intent` relationships are mapped accurately.
